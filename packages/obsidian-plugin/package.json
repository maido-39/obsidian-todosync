{
  "name": "@todomd/obsidian-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian plugin: register dated note tasks to the todomd engine and sync to CalDAV.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "obsidian": "^1.5.7",
    "typescript": "^5.4.5"
  }
}
