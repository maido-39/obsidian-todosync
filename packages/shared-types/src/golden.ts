/**
 * Contract version for the language-neutral golden corpus
 * (`fixtures/golden/`), shared by the TypeScript core and the future Kotlin
 * port. Bump when the structural shape of `expected.json` changes.
 */
export const GOLDEN_SCHEMA_VERSION = 1;
