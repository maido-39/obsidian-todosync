{
  "name": "todomd",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Markdown-as-SSOT ToDo/calendar system — core format parser/serializer (Increment 1)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "playwright": "1.49.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
